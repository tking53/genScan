add_subdirectory(fitters)
add_subdirectory(utilities)
add_subdirectory(xml)
add_subdirectory(pixie)
add_subdirectory(caen)

#we probably still need to append into the doc lists from here
#but that is seldom used, so I will ignore it until someone asks for it
